# Xiaowe Blog

A Jekyll-powered personal blog for GitHub Pages.

## Structure
- `_posts` for articles
- `_layouts` and `_includes` for templates
- `assets` for styles and scripts

## Local preview (optional)
Install Ruby + Jekyll, then run:

```bash
jekyll serve
```

GitHub Pages builds the site automatically after you push.
